use cpu_cache_sim::cache;
use cpu_cache_sim::stats;

fn main() {
    // TODO: parse CLI args (cache size, associativity, block size, trace file)
    // TODO: build a Cache from config
    // TODO: read memory access trace and feed each address to cache.access()
    // TODO: print stats (hits, misses, hit rate) from stats::Stats
    todo!()
}
